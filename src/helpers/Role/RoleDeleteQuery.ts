// import { database } from "../../database/database";

// export const deleteRoleQuery = (id: number) => {
//     return new Promise(async (resolve, reject) => {
//         try {
//             await database.role.delete({
//                 where: { Id: id }
//             });
//             resolve(true);
//         } catch (error) {
//             console.error('deleteRoleQuery');
//             reject(false);
//         }
//     });
// };
